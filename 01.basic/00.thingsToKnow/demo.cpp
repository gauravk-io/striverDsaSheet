#include<bits/stdc++.h>
using namespace std; //When you include this directive in your code, you no longer need to prefix standard library entities with std::.
// For example, instead of writing std::cout, you can simply write cout. using namespace std; allows you to use cout and endl without prefixing them with std::.

int main() {
    int x , y;

    cin >> x >> y; // cin is for taking input.
    cout << "Value of x: " << x << " and y: " <<y; //cout is for output.
    return 0;
}
